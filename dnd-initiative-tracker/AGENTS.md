# Requirements

- Use millimeters for all dimensions.
- Keep automatic name fitting and wrapping inside the SCAD file, not in shell
  code.
- The base text should be separate objects so that 3D slicers can print them in different colors
- Backwards-compat is not important
